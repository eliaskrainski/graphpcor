% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dtgUtils.R
\name{dtg}
\alias{dtg}
\title{Set a graph whose nodes represent two kind of
variables: children and parent. See details.}
\usage{
dtg(...)
}
\arguments{
\item{...}{a list of formula used as relationship
to define the Directed Tree Graph - DTG.
Parent nodes shall be in the right side while children
(or parent with a parent) in the left side.
Please see the examples.}
}
\description{
Set a graph whose nodes represent two kind of
variables: children and parent. See details.
}
\details{
The children variables are those with an ancestor (parent),
and are identified as \code{c1}, ..., \code{cn}, where \code{n} is the
total number of children variables.
The variables are identified as \code{p1}, ..., \code{pm},
where the \code{m} is the number of parent variables.
The main parent (fist) should be identified as \code{p1}.
Parent variables (exept \code{p1}) have an ancestor,
which is a parent variable.
}
\examples{
g1 <- dtg(p1 ~ c1 + c2 - c3)
g1
summary(g1)
plot(g1)
precision(g1)
precision(g1, theta = 0)

g2 <- dtg(p1 ~ c1 + c2 + p2,
          p2 ~ c3 - c4)
g2
summary(g2)
plot(g2)
precision(g2)
precision(g2, theta = c(0, 0))

g3 <- dtg(p1 ~ -p2 + c1 + c2,
          p2 ~ c3)
g3
summary(g3)
plot(g3)
precision(g3)
precision(g3, theta = c(0,0))
}
