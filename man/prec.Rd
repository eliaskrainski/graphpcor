% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_methods.R, R/param_prec.R
\name{prec}
\alias{prec}
\alias{prec.default}
\alias{prec.inla}
\alias{vcov,Matrix-method}
\alias{Lprec}
\alias{fillLprec}
\alias{theta2correl}
\title{The \code{prec} method}
\usage{
prec(model, ...)

\method{prec}{default}(model, ...)

\method{prec}{inla}(model, ...)

\S4method{vcov}{Matrix}(object, ...)

Lprec(theta, p, ilowerL, d0)

fillLprec(L, lfi, ilowerL)

theta2correl(theta, p, ilowerL, d0, transf = c("natural", "cpc"))
}
\arguments{
\item{model}{a model object}

\item{...}{additional arguments}

\item{object}{Matrix supposed to be a
sparse precision matrix}

\item{theta}{numeric vector of length \code{m}
with the parameter}

\item{p}{integer giving the dimention of Q.
If \code{p} and \code{ilowerL} are missing, then
Q is assumed to be dense and
\code{p = (1+sqrt(1+8*length(theta)))}.}

\item{ilowerL}{integer vector as index
to (lower) L to be filled with \code{theta}.}

\item{L}{matrix as the lower triangle
containing the Cholesky decomposition of
a precision matrix}

\item{lfi}{integer vector used as indicator of the
position in the lower matrix where are the
fill-in elements. Must be col then row ordered.}
}
\value{
a precision matrix

matrix as the Cholesky factor of a
precision matrix as the inverse of a correlation

a matrix whose elements at the lower
triangle are the filled in elements of the
Cholesky decomposition of a precision matrix
and diagonal elements as \code{1:p}.
}
\description{
The \code{prec} method

Precision matrix parametrization helper functions.
}
\details{
The precision matrix definition consider
\code{m} parameters for the lower part of L.
If Q is dense, then \code{m = p(p-1)/2}, else
\code{m = length(ilowerL)}.
The precision is defined as
\eqn{Q(\theta) = L(\theta)L(\theta)^T}
}
\section{Methods (by class)}{
\itemize{
\item \code{prec(default)}: The default precision method
computes the inverse of the variance

\item \code{prec(inla)}: Define the prec method for an inla output object

}}
\section{Functions}{
\itemize{
\item \code{vcov(Matrix)}: The \code{vcov} method for sparse matrices

\item \code{fillLprec()}: Function to fill-in a Cholesky matrix

\item \code{theta2correl()}: Build a correlation matrix from the
precision parametrization

}}
\examples{
theta1 <- c(1, -1, -2)
Lprec(theta1)
theta2 <- c(0.5, -0.5, -1, -1)
Lprec(theta2, 4, c(2,4,7,12))
}
