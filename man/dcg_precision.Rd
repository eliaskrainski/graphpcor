% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcgUtils.R
\name{dcg_precision}
\alias{dcg_precision}
\title{Function to build the precision matrix for dcg tree.}
\usage{
dcg_precision(dcg, theta, s.children = NULL, debug = FALSE, new = TRUE)
}
\arguments{
\item{theta}{log of the conditional variances of the
parent variables}

\item{s.children}{vector s (one for each children).
Default is null and it is taken as (+1 or -1) from the formula.}
}
\value{
a precision matrix for given dcg tree and parameters
}
\description{
Function to build the precision matrix for dcg tree.
}
\examples{
dcg1 <- list(
     p1 ~ p2 - p3 + c1 + c2,
     p2 ~ c3 + c4,
     p3 ~ -c5 + c6)
Q1 <- dcg_precision(dcg1, theta = c(0, 0, 0))
cov1 <- chol2inv(chol(Q1))
round(100 * cov2cor(cov1[1:6, 1:6]))

dcg2 <- list(
     p1 ~ p2 + c1 + c2,
     p2 ~ -p3 + c3 + c4,
     p3 ~ -c5 + c6)
Q2 <- dcg_precision(dcg2, theta = c(0, 0, 0))
cov2 <- chol2inv(chol(Q2))
round(100 * cov2cor(cov2[1:6, 1:6]))
}
