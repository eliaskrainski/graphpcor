% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_classes.R, R/basecor.R, R/basecor_utils.R
\docType{class}
\name{basecor-class}
\alias{basecor-class}
\alias{basecor.numeric}
\alias{basecor.matrix}
\alias{print.basecor}
\alias{theta2L}
\title{Information for a base model for correlation matrices}
\usage{
\method{basecor}{numeric}(
  base,
  p,
  parametrization = "cpc",
  decomposition = "svd",
  itheta,
  d0,
  ...
)

\method{basecor}{matrix}(
  base,
  p,
  parametrization = "cpc",
  itheta,
  d0,
  decomposition = "svd",
  ...
)

\method{print}{basecor}(x, ...)

theta2L(theta, p, parametrization = "cpc", itheta, d0)

theta2L(theta, p, parametrization = "cpc", itheta, d0)
}
\value{
a \code{basecor} object

matrix with lower triangle as the Cholesky factor
of a correlation matrix if parametrization is
"cpc" or "sap" and of a precision matrix if
parametrization is 'itp' (with 'd0' as the diagonal elements).

matrix with lower triangle as the Cholesky factor
of a correlation matrix if parametrization is
"cpc" or "sap" and of a precision matrix if
parametrization is 'itp' (with 'd0' as the diagonal elements).
}
\description{
Information for a base model for correlation matrices
}
\section{Functions}{
\itemize{
\item \code{print(basecor)}: Print method for 'basecor'

\item \code{theta2L()}: Cholesky (lower triangular) matrix from theta.

\item \code{theta2L()}: Cholesky (lower triangular) matrix from theta.

}}
\examples{

## Base correlation matrix
c0 <- matrix(c(1,.9,.5, .9,1,.2, .5,.2,1), 3)

## build the 'basecor'
pc.c0 <- basecor(base = c0) ## base as matrix
pc.c0

## using 'theta' instead (numerically the same)
th0 <- pc.c0$theta
pc.th0 <- basecor(base = th0) ## base as vector
pc.th0

all.equal(pc.c0, pc.th0)

## other way around
all.equal(pc.th0, basecor(base = pc.th0$base))

## ITP
th <- c(0.5,-1,0.5,-0.3)
ith <- c(2,3,8,12)
pc2 <- basecor(base = th, p=4, parametrization='itp', itheta = ith)
pc2

Sparse(solve(pc2$base), zeros.rm = TRUE)

}
