% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphUtils.R
\name{graph2H}
\alias{graph2H}
\title{Evaluate the hessian of the KLD for graph model
around a base model.}
\usage{
graph2H(graph, base, method = c("eigen", "svd", "chol"))
}
\arguments{
\item{graph}{model definition of a graphical model.
This can be either a matrix or a 'corgraph'.}

\item{base}{reference correlation matrix
or lower triangular parameters of a 'corgraph' model.}

\item{method}{the decomposition method used to
compute H^0.5 and H^(1/2).}
}
\value{
list containing the hessian,
its 'square root', inverse 'square root' along
with the decomposition used
}
\description{
Evaluate the hessian of the KLD for graph model
around a base model.
}
\examples{
g <- corgraph(x ~ y+z, y ~ v, v ~ z)
ne <- dim(g)
gH0 <- graph2H(g, rep(-1, ne[2]))
## alternatively
Q0 <- precision(g, theta = rep(c(0,-1), ne))
C0 <- cov2cor(solve(Q0))
all.equal(graph2H(g, C0), gH0)
}
