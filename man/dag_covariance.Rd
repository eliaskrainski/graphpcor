% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Utils.R
\name{dag_covariance}
\alias{dag_covariance}
\title{Function to compute the covariance between the
children variables for a given dag and the
parameters of the parents}
\usage{
dag_covariance(dag, theta, s.children = NULL)
}
\arguments{
\item{dag}{the DAG}

\item{theta}{numeric with v_j}

\item{s.children}{numeric with s_i}
}
\description{
Function to compute the covariance between the
children variables for a given dag and the
parameters of the parents
}
\examples{
dag1 <- list(
     p1 ~ p2 + c1 + c2,
     p2 ~ -c3 + c4)
dag_covariance(dag1, c(0, 0))
dag_covariance(dag_rlp(dag1), 0)
}
