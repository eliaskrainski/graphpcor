% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_methods.R, R/pcor.R
\name{pcor}
\alias{pcor}
\title{Information needed for a prior that penalizes
the divergence from a base correlation matrix.}
\usage{
pcor(base, p, parametrization, itheta, d0)
}
\arguments{
\item{p}{integer with the dimention, number of rows
and columns of the correlation matrix.}

\item{parametrization}{character to specify the
parametrization choice: "ITP" (or "itp"),
"SAP" (or "sap"), "CPC" (or "cpc"), see details.}

\item{itheta}{integer vector to specify the position
'theta' will be placed in the (initial, before fill-in)
Cholesky factor in the IT. Default is missing.}

\item{d0}{numeric vector to specify the diagonal
for the (initial) Cholesky factor in the ITP.
Default consider 'd0=p:1'.}

\item{theta}{numeric vector with length equal 'm'.
In the dense case 'm=p(p-1)/2' whereas in the
sparse case 'm=length(itheta)'.}
}
\value{
a pcor object
}
\description{
Information needed for a prior that penalizes
the divergence from a base correlation matrix.

Correlation prior and helper functions
to work with correlation matrices.
}
