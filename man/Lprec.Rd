% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_prec.R
\name{Lprec}
\alias{Lprec}
\alias{fillLprec}
\title{Build the Cholesky factor, L, of a precision matrix.}
\usage{
Lprec(theta, p, itheta, d0)

fillLprec(L, lfi)
}
\arguments{
\item{theta}{numeric vector of length \code{m}
with the parameters.}

\item{p}{integer giving the dimension of
the precision matrix.}

\item{itheta}{integer vector as index
to (the lower part of) L
to be filled with \code{theta}.
Length of itheta must be equal 'm'.}

\item{d0}{elements at the diagonal of L.
By default uses \code{p:1}.}

\item{L}{matrix as the lower triangle
containing the Cholesky decomposition of
a precision matrix}

\item{lfi}{integer vector used as indicator of the
position in the lower matrix where are the
fill-in elements. Must be col then row ordered.}
}
\value{
matrix as the (lower triangle)
Cholesky factor of a precision matrix.
}
\description{
Build the Cholesky factor, L, of a precision matrix.
}
\details{
The precision is defined as
\eqn{Q(\theta) = L(\theta)L(\theta)^T}
where \eqn{L(\theta)} the Cholesky of \eqn{Q(\theta)}
is filled using the following steps
\enumerate{
\item Build \eqn{L} starting with filling its diagonal
with 'd0'.
\item Place \eqn{theta} at the positions 'itheta'
\item Compute the fill-in elements
}
}
\section{Functions}{
\itemize{
\item \code{fillLprec()}: Function to fill-in a Cholesky matrix

}}
\examples{
Lprec(c(1,-1,-2), 3, c(2,3,6))
Lprec(c(2,-1,1,-0.5), 4, c(2,3,8,12))
}
\seealso{
\link{correl}
}
